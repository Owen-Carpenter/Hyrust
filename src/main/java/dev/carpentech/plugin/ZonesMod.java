package dev.carpentech.plugin;

import dev.carpentech.plugin.zone.ZoneManager;
import dev.carpentech.plugin.zone.ZoneListener;

// TODO: Add correct imports for Hytale API
// import com.hypixel.hytale.server.core.mod.ModInitializer;
// import com.hypixel.hytale.server.core.mod.ModContext;

public class ZonesMod { // implements ModInitializer

    // TODO: Uncomment when Hytale API is available
    /*
    @Override
    public void onInitialize(ModContext ctx) {

        // Initialize zones once world loads
        ctx.getServer().onWorldLoad(world -> {
            ZoneManager.initialize(world);
        });

        // Register event listeners
        ctx.getEvents().register(new ZoneListener());
    }
    */
}